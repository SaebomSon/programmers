SELECT * FROM PLACES WHERE HOST_ID IN(SELECT HOST_ID FROM PLACES GROUP BY HOST_ID HAVING COUNT(HOST_ID) > 1) ORDER BY ID

WHERE절에서는 COUNT함수를 사용할 수 없기 때문에, SUB QUERY를 통해서 COUNT(HOST_ID)가 1개 이상인 HOST_ID를 구하고 기본 QUERY문의 WHERE절을 통해 확인 한다.(WHERE ~ IN ~)